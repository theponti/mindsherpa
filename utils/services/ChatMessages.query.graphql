query ChatMessages($chatId: String!) {
  chatMessages(chatId: $chatId) {
    summary {
      text
    }
    messages {
      id
      message
      role
      chatId
      profileId
      createdAt
    }
  }
}
